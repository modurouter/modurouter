> Current behavior: visitors automatically enter a private guest session. Configured Google and administrator login remain optional. Historical verification notes below describe the version tested at that time, including the former guest flow. See README for current input limits and billing recovery.

# Phase 1 delivery and acceptance

Source: Google document `1ESO4ZnpFlq2CiH4bcgtioTTkis89nmnopPMj-QOT_WY`, tabs `t.0` and `t.c3c1sns23or0`, read 2026-09-18.

## Confirmed scope decisions

- The product is a Korean AI harness. OpenRouter is the only LLM provider.
- Web deployment uses `https://modurouter.vercel.app`, as requested by the user. Browser API and auth traffic use same-origin proxying to the VPS, with OAuth callback on the public web origin. The source TRD now records this deployment topology.
- The user explicitly requests no database backups. Do not configure backup jobs or create recovery copies. The source PRD and TRD now use DB restart data persistence as the acceptance check instead of backup restoration.
- Visitors can use a guest session immediately, or sign in through configured Google OAuth or `/admin` password login. Guests share the configured daily request and cost limits.
- The user removed the provider-key spending cap and the internal platform daily dollar cap from Phase 1. Per-user request and cost limits remain enforced. The platform ledger continues to record total spending.
- The user authorized removal of old VPS application workloads and data before deploying the new MariaDB and FastAPI stack. Retain essential operating system and SSH access.
- Root `.env` is the only manually maintained configuration source, mode 600. Never source it as a shell script. Generated deployment configuration is derived from it. No `.env.example`.

## Acceptance checklist

| Task | Required evidence | Status |
| --- | --- | --- |
| T01 Scope | Korean learning question, sourced web answer, PDF summary, image OCR, voice input and readout demo flows; exclusions recorded | Defined and implemented; voice prototype evidence is under T09 |
| T02 Deployment | Next.js on Vercel; fresh Compose proxy/API/worker/MariaDB on VPS; TLS; persistent DB restart | Complete. Runtime, HTTPS, DB restart persistence and readiness verified on the selected same-origin domain |
| T03 Identity | OIDC state, nonce, PKCE, signature validation; hashed sessions; CSRF; ownership isolation; logout and expiry | On hold for live Google login per user choice. Session, CSRF, expiry, logout, guest isolation and deletion verified. The real Authlib flow generated state, nonce and PKCE, accepted a locally signed test ID token, issued a host-only session, and rejected invalid state and a forged signature. Callback targets the public web origin |
| T04 Model catalog | Atomic snapshots; decimal pricing; invalid price rejection; refresh failure vs stale state | Verified by catalog/staleness tests and a live 246-model sync |
| T05 Routing | Quality allowlist; both price ceilings; context limits; one eligible pre-output fallback; no fallback on 401/402/refusal | Verified by quality samples, pricing/context tests, one-fallback and no-account-error-fallback tests |
| T06 Harness | Streaming, cancel, history, deletion, idempotency, restart interruption, strict tool plans and bounded execution | Verified streaming, idempotency, partial-response cancellation, restart interruption, native/text tool-plan validation and protocol suppression |
| T07 Web tools | DuckDuckGo search; bounded public URL fetching; DNS and redirect SSRF defense; grounded sources and honest errors | Verified live search and page retrieval, collected-source links, private DNS and redirect rejection. Forced search outage produced an explicit error. A failed search-result page read leaves a durable limitation note in the saved answer; external search availability is not guaranteed |
| T08 Attachments | Ownership, MIME checks, bounded TXT/PDF/OCR worker, preview, expiry and deletion | Verified Korean PDF and OCR demos, MIME/size/ownership limits, expiry deletion and worker timeout paths |
| T09 Voice | Korean recognition with editable result; explicit consent; synthesis stop; unsupported and denied paths | Complete for the supported-browser prototype. Native Chrome recognition transcribed a Korean audio track into the production input field. The text was edited, sent, answered and reopened after reload. Native Korean synthesis emitted start and end events; stop, consent, denied and unsupported paths were also checked. Physical microphone and speaker quality were not measured |
| T10 Interface | Peer Design/Base UI; 360/768/1440 widths; light/dark; keyboard and focus; no horizontal overflow | Verified all three widths in light and dark modes, no horizontal overflow, dialog focus return and keyboard operation |
| T11 Accounting | Locked daily user and shared guest limits with platform cost accounting; every model attempt; idempotent settlement; unknown cost retained; Seoul reset | Complete. Per-user and shared guest limits, late settlement after guest deletion, races, platform totals and token/cost display verified. Generation IDs are captured from response headers before streamed content |
| T12 Integration | Functional and failure scenarios, concurrent quota races, load/resource observations, honest quality and cost evidence | Complete for the user-approved scope. Five concurrent chats, OCR, native-browser voice, guest auto-start, persistence, deployment and failure paths passed. Evidence is recorded below |
| VPS security | Fail2ban installed, enabled and enforcing SSH jail; essential access retained; old workloads removed | Verified: password SSH access restored; old services and application paths removed; Fail2ban active/enabled and nftables ban/unban verified |

## Explicit exclusions

No payments, subscriptions, government subsidy settlement, other LLM providers, hosted models, autonomous browser control, arbitrary local filesystem access, scanned-PDF OCR, or image scene understanding. Voice is a supported-browser prototype. No claims of savings or measured response performance without evidence.

## UI direction

Quiet neutral workspace with square edges and a four-pixel spacing rhythm. The conversation is the primary surface, with a compact history sidebar and secondary usage details. Gentle drawer and dialog transitions preserve focus and respect reduced motion. No marketing hero or ornamental cards.

## Operational limitations

A single VPS outage interrupts the service. By explicit user choice, no database backups are made; loss of the persistent volume is not recoverable by this application. Google client credentials are absent and live Google login is skipped per user instruction. New visitors can use the service without signing in. Platform and provider-key dollar caps are omitted by user choice, while member limits remain active.

## Current verification evidence

- 2026-09-18: API deployed to the freshly cleared VPS behind Caddy HTTPS. Live and ready endpoints returned HTTP 200. MariaDB is internal-only. Worker collected 246 valid model entries.
- Next.js deployed to https://modurouter.vercel.app; public page and same-origin configuration API returned HTTP 200.
- The full 60-test backend suite passed against local MariaDB, including quota concurrency, guest isolation, cancellation, fallback, idempotency, SSRF validation, search-outage reporting, file restrictions, OAuth state/nonce/PKCE and invalid-signature handling.
- Peer Design 1.7.0 doctor passed. Base UI 1.7.0 installed. Source examples: `examples/base-ui/components/alert-dialog/demos/hero/css-modules/index.tsx` and `examples/base-ui/components/dialog/demos/hero/css-modules/index.tsx` in the local Peer Design repository.
- Authenticated 360px, 768px and 1440px screenshots inspected in both light and dark modes. Document width equals viewport width. Escape returns focus from mobile and deletion dialogs. Physical microphone and speaker quality remain unverified.
- Live Korean model samples, grounded summary and strict tool-plan samples ran through reserved and settled attempts. A fabricated source-number case led to an incremental citation filter. Korean OCR and PDF questions subsequently completed with correct extracted facts. The first live PDF planner failure was fixed by making the planner role and JSON-only response explicit.

## Production evidence, 2026-09-18 KST

- Five simultaneous Korean chat requests and one Korean image extraction completed. First SSE event: 0.430 to 0.979 seconds. First model text: 2.246 to 2.714 seconds. Complete request: 3.119 to 3.835 seconds. These are five observations, not a performance guarantee.
- Per-request confirmed costs in that sample: 0.0000310586, 0.0000318308, 0.0000382014, 0.0000410972 and 0.0000347265 USD. No pending reservations remained for these five requests.
- Two container samples during the workload observed API memory up to 82.39 MiB, worker memory up to 71.5 MiB, database memory up to 99.96 MiB and proxy memory up to 14.02 MiB. Short sampling does not establish peak or sustained-load capacity.
- Korean OCR recovered both lines of the synthetic library notice exactly. PDF extraction preserved the Korean text and the final answer correctly reported Monday closure, five books and fourteen days.
- Live DuckDuckGo search returned five snippets and the harness read a public page. Links were limited to collected sources. One answer over-interpreted a library calendar as recurring closure dates; the prototype must not be presented as factually reliable solely because a source link exists.
- Direct API disconnect after a text delta produced `cancelled`, preserved the partial response and held 0.0012417500 USD pending reconciliation. Retrying the same key returned the same run and one attempt. The worker later reconciled the provider charge. Vercel could keep an upstream request alive after transport disconnect, so page exit now sends an explicit best-effort cancel request.
- The production DB restart retained users, conversations and messages (counts 1, 2 and 4 at that checkpoint). Readiness returned 200 three times after recovery. The worker now retries transient dependency failures, and the API uses asyncio explicitly to avoid the observed uvloop/driver shutdown fault.
- File-expiry tests actually removed the original and cleared extracted text. Timeout tests verified child termination and a retryable extraction error. Ownership tests returned 404 for another user's conversation and attachment.
- Deleting a guest with pending provider cost exposed a shared-reservation accounting issue. Guest classification is now retained without identity data after deletion; a regression test settles the delayed charge and verifies the shared reservation is released.
- Screenshots and synthetic-load evidence formerly under `.ops/` were removed during cleanup. `.ops/` now permits only `deploy.sh` and `run.sh`; future artifacts belong in ignored `server/.runtime/` or the system temporary directory. No backup, recovery copy or off-VPS DB export was created.

- Browser page-exit cancellation was verified through Vercel after waiting for the model phase: reload resulted in `cancelled`, one attempt and a retained pending reservation, with no automatic regeneration.
- In headless Chrome, native `webkitSpeechRecognition` received a synthetic Korean speech track generated by macOS speech output. It transcribed “도서관은 매주 월요일에 쉽니다” exactly, and the production UI placed that text in its editable composer without sending it. This tests the real browser recognition service and app integration, not the physical microphone.
- A live browser exposed nine Korean system voices. Its native `speechSynthesis` entered the speaking state after the user clicked `읽어주기` and stopped after `읽기 중단`; no API stub was used. Audible speaker output was not measured. The initial voice list can load asynchronously, so the client now waits briefly for `voiceschanged` before reporting no Korean voice.
- The deployed voice-loading fix was exercised by delaying the browser's visible voice list for 700 ms. The button entered the waiting state, native synthesis began after `voiceschanged`, no alert appeared, and the stop button ended speech. The latest Vercel build, public page and same-origin config endpoint returned successfully.
- Password-based SSH from root `.env` succeeded again with key and agent lookup disabled; Fail2ban was active. OpenRouter's current-key endpoint returned HTTP 200 with no configured key limit or reset. The user later removed the provider-key cap requirement.
- The OAuth callback mismatch found during the final specification audit was fixed and deployed. The public web and configuration routes plus API live and ready checks returned HTTP 200 after deployment. Live OAuth remains untested because Google client credentials were explicitly skipped.
- The deployed voice demo used Chrome's native Korean recognition on a synthetic speech track. The recognized text was edited, sent through the live guest flow and answered in Korean. Reloading and reopening the conversation recovered both saved messages. Native `speechSynthesis` on the same answer emitted `start` and `end` events with no error. This verifies the browser integration, while physical microphone pickup and speaker audibility remain unmeasured.
- A read-only VPS audit found four Modurouter containers, active and enabled Fail2ban with its SSH jail, no old Tommy/Hermes/Eden/Joypl service units or application paths in the checked locations, and no Modurouter backup schedule.
- An OIDC audit found that a rejected token signature returned generic HTTP 503 instead of the login-error page. The callback now catches the JWT library's validation error without relaxing verification. The real authorization start flow was checked for state, nonce, PKCE S256 and the public callback URL; a mismatched state and invalid signature both return the login-error redirect. This fix passed the full suite and was deployed with live and ready checks at 200.
- An end-to-end local OIDC test used an ephemeral RSA key and a correctly signed ID token. The callback created a host-only HttpOnly session and `/v1/me` returned the test identity. A second token signed by an untrusted key was rejected and did not create a session. The full 57-test suite passed after this test was added.
- A later ledger audit found one cancelled attempt with 0.0012420000 USD still reserved after about 29 minutes and no generation ID. The guest and platform ledger totals matched. The worker cannot query OpenRouter usage without an ID, so the reservation remains pending rather than being counted as free. OpenRouter documents an [`X-Generation-Id` response header](https://openrouter.ai/docs/guides/features/response-caching) on every response; the adapter now records it before reading streamed content. A header-only cancellation regression and adapter test passed in the 59-test suite, and the deployed API live and ready checks returned 200. An attempt cancelled before any response headers can still lack an ID.
- A fresh live production isolation check created two guest sessions with distinct accounts. Another account's conversation and attachment both returned 404. A file named PNG with plain-text bytes returned 422; a valid UTF-8 text file uploaded successfully. Both test accounts were then deleted through the public web origin, each returning 200.
- A follow-up VPS audit showed all six file-deletion jobs completed, with no extracted text remaining on deleted accounts. Existing files on active accounts were left in place.
- A forced DuckDuckGo outage in the integration harness produced `SEARCH_UNAVAILABLE`, no answer delta and no model call. The run remained `failed`; it did not claim to have read a source. The complete suite passed with 60 tests.
- The domain audit found no owned custom domains in the connected Vercel account; the project has only `modurouter.vercel.app`. [Vercel states](https://vercel.com/kb/guide/can-i-set-a-cookie-from-my-vercel-project-subdomain-to-vercel-app) that `vercel.app` is a public suffix and project subdomains cannot share an apex-domain cookie. The selected deployment therefore uses the verified same-origin proxy, now recorded in the source TRD.
- The client also supports a distinct HTTPS API origin for ordinary calls, streaming, cancellation, login and logout. An alternate-origin build included the API address and had zero Vercel rewrites; the selected same-origin build retained both proxy rewrites. The backend OAuth callback and deployment configuration use the configured public API origin, while a CORS preflight test checked credentialed access from the web origin.
- The two-mode domain change passed 61 backend tests, Ruff, the client typecheck and both build configurations. It was deployed with the same-origin default. Public web and configuration routes returned 200, unauthenticated `/v1/me` returned the expected 401, and the VPS API live and ready routes returned 200. Root contents and `.env` mode 600 remained as requested.
- An operations audit closed a logging gap for cancelled and failed runs. Every terminal run now logs request and run IDs, status, model, error code, confirmed and pending cost, attempt count and latency without the user prompt. Worker jobs log queue age and processing latency without payloads. A read-only aggregate command reports price freshness, platform spending, pending charges and queued jobs. The full 62-test backend suite and Ruff passed, and the new server image returned live and ready 200.
- A production guest run completed and its test account was deleted. The sanitized terminal log recorded one model attempt, 0.0000178362 USD confirmed cost, no pending cost and 3326 ms latency with both IDs present. A separate synthetic TXT extraction reached `ready`, then the account was deleted; worker logs recorded extraction and deletion job delays. The aggregate check showed a fresh price snapshot, zero queued/running/failed jobs, and one older pending attempt without a generation ID. Its 0.0012420000 USD reservation remains held rather than falsely settled.
- Search-result page failures were rechecked against the TRD's honest-error requirement. The prior transient warning could disappear from the final saved answer. The harness now tells the model that some original pages were unreadable and appends a durable note describing whether it used only snippets or also opened pages. An integration test forced `PAGE_UNAVAILABLE` and verified the warning event and saved answer. Another test put malicious instructions in a page body and verified that they reached the model only as explicitly marked untrusted source data. The full 64-test backend suite and Ruff passed; the new API image was deployed and public web/config and API live/ready returned 200.
- At the first source PRD reconciliation, nine tasks were marked complete, two remained in progress and T03 was on hold by user choice. Completed titles changed from blue to gray with strikethrough, while incomplete titles stayed blue. PRD T12 and the TRD deployment and launch criteria now require DB restart data persistence in place of backup restoration. The TRD explicitly records the no-backup decision and the resulting data-loss risk.
- The user-selected `modurouter.vercel.app` topology was reflected in the source TRD. PRD T02 was marked complete after public HTTPS, API readiness and the production DB restart persistence check; its heading is gray with strikethrough.
- PRD T09 was marked complete because its supported-browser STT, TTS, editing, stop, denied and unsupported acceptance paths were verified in the deployed app. Physical microphone and speaker quality remain an unmeasured limitation rather than an original Phase 1 acceptance condition.
- PRD T03 is on hold for live Google login, as the user directed when credentials were unavailable. The PRD scope records immediate guest access as the demo path.
- A fresh VPS audit found Fail2ban active and enabled with its SSH jail, exactly the four Modurouter containers, no old named service units or application paths, no backup schedule, and no legacy web or database server packages. Public web/config and API live/ready endpoints all returned 200; the operations report showed a fresh price snapshot, zero queued or failed jobs, and one conservatively reserved attempt without a generation ID.
- Final verification: web, same-origin config, API liveness and API readiness all returned 200. Root topology matched the seven requested entries, `.env` was mode 600, and scanning application source plus build outputs found no configured credential values. Seven synthetic deleted guest accounts were reclassified without identity data and the shared ledger was reconciled with platform totals under the admission lock.
- A fresh browser session on the public site automatically entered guest mode without clicking a login button. A Korean question completed, its conversation survived reload, and deleting the test account returned the page to the manual guest-start state. A new live HTTP check confirmed a session-only guest cookie, the 30-request and 0.10 USD shared guest limits, and account deletion.
- The internal platform dollar setting was removed from root `.env`, Compose, the backend and deployment selection. Migration `0002` made platform bucket limits nullable; production has one uncapped platform row, no capped platform rows and no uncapped user or guest rows. The runtime has no platform cap environment variable. Aggregate status reports `limit_usd: null` and `remaining_usd: null` while retaining spending and the one unresolved reservation.
- Final local verification passed 77 backend tests and Ruff. The test schema is rebuilt once and cleared between tests to avoid intermittent MariaDB DDL collisions. Public web and configuration endpoints and direct API live and ready endpoints returned 200. Fail2ban is active and enabled with its SSH jail, four current containers are running, and the checked old service paths and backup schedules remain absent. Root contents match the seven requested entries, and `.env` remains mode 600.

## Final scope reconciliation

The source PRD marks T11 and T12 complete after the user removed the provider-key and internal platform dollar caps. Member request and cost limits remain active. Live Google OAuth and all database backups remain excluded by explicit user decisions. Physical microphone pickup and speaker audibility were not measured.
